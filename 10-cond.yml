- name: ansible to write conditions
  hosts: web 
  vars: 
   is_alive: false
  tasks: 
    - name: ansible to  write 
      ansible.builtin.debug: 
        msg: "{{hi my name is naveen }}"
      when: is_alive == false