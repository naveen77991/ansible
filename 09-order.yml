- name: ANSIBLE TO ORDER 
  hosts: web  
  vars: 
    course: task 
  vars_files: 
   - course.yml    
  vars_prompt: 
   - name: course 
     prompt: "plz eneter ur username"
      

  tasks: 
    - name: ansible to vars  
      
      ansible.builtin.debug: 
        msg: "hi my course is {{ course }}"