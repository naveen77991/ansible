- name: ANSIBLE TO ORDER 
  hosts: web  
  vars: 
    course: task 
   
  
      

  tasks: 
    - name: ansible to vars  
      
      ansible.builtin.debug: 
        msg: "hi my course is {{ course }}"